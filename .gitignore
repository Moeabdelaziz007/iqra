.env
.env.local
.env.*
!.env.example

# Dependencies
node_modules/
dist/
.next/

# OS
.DS_Store
*.log
*.tmp
npm-debug.log*

# IQRA Sovereign Data — never commit raw training data or secrets
# كل محتوى .iqra/ محلي — لا يُرفع أبداً
.iqra/
.iqra_loop_state

# reward ledger — بيانات محلية
iqra-core/data/reward_ledger.jsonl
iqra-core/data/*.jsonl
iqra-core/data/*.json

# Build artifacts
raw_api_payload*.json
*.bak

# MCP local config (may contain tokens)
.ag/mcp.json

# Git temp files
.git_commit_msg.tmp
trust_fallback.log
